export class User {
    constructor(public name: string, public login:string, public dp_url:string, public repos:number, public html_url:string, public created_on:Date, public followers:number, public following:number){
        
    }
}
